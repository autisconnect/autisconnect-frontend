import { Alert, Card, ProgressBar } from 'react-bootstrap';
const label = (percentage) => percentage === null ? 'Sem limite contratado' : `${percentage.toFixed(0)}% utilizado`;
const variant = (percentage) => percentage >= 100 ? 'danger' : percentage >= 90 ? 'warning' : percentage >= 80 ? 'info' : 'success';
const SubscriptionUsage = ({ title, data, unavailable = false }) => <Card className="h-100"><Card.Body><Card.Title as="h2" className="h6">{title}</Card.Title>{unavailable ? <div className="text-muted">Consumo indisponível até existir vínculo de arquivos com a clínica.</div> : <>{data.limit === null ? <div className="text-muted">{data.used} utilizado(s) - sem limite</div> : <><div className="mb-2">{data.used} de {data.limit}</div><ProgressBar now={data.percentage || 0} variant={variant(data.percentage || 0)} label={label(data.percentage)} />{data.percentage >= 100 && <Alert className="mt-3 mb-0" variant="danger">Limite atingido.</Alert>}{data.percentage >= 90 && data.percentage < 100 && <Alert className="mt-3 mb-0" variant="warning">Consumo acima de 90%.</Alert>}{data.percentage >= 80 && data.percentage < 90 && <Alert className="mt-3 mb-0" variant="info">Consumo acima de 80%.</Alert>}</>}</>}</Card.Body></Card>;
export default SubscriptionUsage;
